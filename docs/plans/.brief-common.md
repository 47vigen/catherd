# Brief shared by the plan writers

You are writing ONE implementation plan for catherd, a new open-source TypeScript package in `/Users/vigen/agora/lab/catherd`. Do not write source code files, run the implementation, or commit. The only file you create is your plan.

## Read first, in this order
1. The writing-plans skill, whose format you must follow exactly: header, Global Constraints, Review Focus, File Structure, bite-sized TDD tasks with complete code, no placeholders, and a self-review at the end. File: `/Users/vigen/Library/Application Support/Claude/local-agent-mode-sessions/25a26fad-930e-4cc4-b286-251c9c5d3ffc/6a379a0d-3ad9-4e93-be45-49bd8702f492/rpm/plugin_01SpdD9DyAKConhUFSMDiDqa/skills/writing-plans/SKILL.md`. One override: plans live in `docs/plans/`, not `docs/superpowers/plans/`.
2. The spec: `docs/specs/2026-09-24-catherd-design.md`. It is approved, so implement it and do not redesign it.
3. `docs/plans/INTERFACES.md`. Use the names it assigns to your plan exactly. Import other plans' names exactly as written, and never redefine them. If you truly need a cross-plan name that is missing, add it in your own plan's code, and list it in your final report.
4. `docs/plans/2026-09-24-01-core.md`, plan 1, which is written. Match its style: task shape, test helpers (`withHome`, `tempRepo`, `fakeBinPath`), the commit message format with the `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>` trailer, and `CATHERD_HOME` isolation in tests. `src/types.ts` in its Task 2 is the base contract.

## House rules for the code in your plan

**Tooling.** Vigen's standing rule: newest, fastest tools, with Rust-based ones first, latest versions, never pinned. Install with `pnpm add <pkg>` / `pnpm add -D <pkg>`, with no version in the plan text; the lockfile holds versions.
- Node ≥ 22, ESM, strict TypeScript.
- TypeScript is `typescript` latest (7.x, the native compiler), and the typecheck is `tsc --noEmit`.
- Build: **tsdown** (Rolldown), config `tsdown.config.ts`. The shebang lives at the top of `src/cli.ts`.
- Tests: **vitest** latest. Lint: **oxlint**. Format: **oxfmt**. There is no Biome, ESLint or Prettier.
- Git hooks: **lefthook** (`lefthook.yml`), with commitlint on commit-msg and oxlint plus `oxfmt --check` on pre-commit. Release: Changesets.
- Gate: `pnpm test`, `pnpm typecheck`, `pnpm lint` (oxlint) and `pnpm format:check` (`oxfmt --check`).

**Do not reinvent the wheel.** For every job, search npm first for a maintained, modern package, and hand-write only what no package covers. Say so in one line in the task where you hand-write. Every runtime or dev dependency a task adds gets one line in `docs/dependencies.md`, in that task: `- <pkg> — <what it does for catherd> — <why this one>`.

These are already chosen for the whole project; use them, do not pick rivals:

| Job | Package |
|---|---|
| CLI and subcommands | `citty` |
| Child processes | `execa`: `detached`, `stdout: { file }`, `cancelSignal` |
| Config and data dirs | `xdg-basedir` |
| The file lock | `proper-lockfile` |
| File walks and globs | `tinyglobby` |
| HTTP with retries | `ofetch` |
| Schemas | `zod` (v4) |
| MCP | `@modelcontextprotocol/sdk` |
| TUI | Ink + React (latest), unless plan 4's first task proves OpenTUI (`@opentui/core` + `@opentui/react`) runs on Node without Bun |

Other rules:
- No comments that restate code. A comment explains only a non-obvious why.
- Every test that touches config or data uses `withHome()`. No test reaches the network unless it lives in `test/live/` and runs only under `CATHERD_LIVE=1`.
- Secrets: the TypeSafe key never appears in argv, logs, run folders, briefs, errors or snapshots. Tests use a fake key.
- Every step that changes code shows the complete code. "Similar to Task N" is forbidden; repeat the code instead.
- Where a fact about an external tool or package API is not certain, add a short verification step (its `--help`, its README in `node_modules`, or context7 docs) before the code that depends on it. State what the step must confirm and what to change if it does not.

## Final report (your reply to me), at most 25 lines
- The plan's path, and its task count.
- Any cross-plan name you had to add beyond INTERFACES.md.
- Any spec requirement you could not place in a task, and why.
- Any fact you were unsure of, and the verification step that covers it.
