# TUI frames

Generated by rendering each screen through `@opentui/react/test-utils`'s `testRender`, at 80x24
and 120x40, in normal, `--plain` and `--reduced-motion`. For eyeballing only — not a golden-file
test; the real gate is `bun test`.

Two things a plain-text dump cannot show:

- **Colour.** `captureCharFrame()` returns characters only, no ANSI codes, so the ginger-to-pink
  wordmark gradient and the ginger/cream/pink tints all render as plain text here. They are
  covered separately: `test/tui/theme.test.ts` checks `tint()`'s hex output, and Task 12's manual
  colour-depth check (`docs/manual-tests.md`, if the plugin bundle covers it, or run the `for e in
  ...` loop from the plan by hand) checks the real ANSI codes in a pty.
- **The `init` intro.** Every frame below has the intro force-skipped (`reducedMotion: true`
  regardless of the section's own mode), landing straight on the Jev key prompt; the intro's own
  6-frame wordmark reveal is covered by `test/tui/ui.test.tsx`'s `Intro` tests instead, since a
  static frame can only show one of its steps.

## The bare `catherd` (profile editor), idle header with the cat
### normal, 80x24

```
 /\_/\  .  catherd
(=^.^=)/   herds coding agents
 (")(")    profiles · default

profile default 1/1 (active) · p next · n new · c copy · x delete
❯ ▸ 🐾 architect   claude-opus-5-5#high
  ▸ 🐾 verifier    claude-opus-5-5#low
  ▸ 🐾 worker      luna#high, sol#medium, sol#high, sol#xhigh
  ▸ 🐾 reviewer    sol#high
  ▸ 🐾 ui-reviewer sol#medium
  ▸ 🐾 artist      sol#medium
  ▸ 🐾 writer      luna#high
  ▸ 🐾 researcher  luna#high
  objective     cost  (cheapest rung first)
  heavy slots   cpus/2  (for catherd lock)
  🐾 notify on milestone
  🐾 notify on finish
  🐾 notify on blocked
↑↓ move · → open · space ticks · / filter · enter save · q quit
```

### normal, 120x40

```
 /\_/\  .  catherd
(=^.^=)/   herds coding agents
 (")(")    profiles · default

profile default 1/1 (active) · p next · n new · c copy · x delete
❯ ▸ 🐾 architect   claude-opus-5-5#high
  ▸ 🐾 verifier    claude-opus-5-5#low
  ▸ 🐾 worker      luna#high, sol#medium, sol#high, sol#xhigh
  ▸ 🐾 reviewer    sol#high
  ▸ 🐾 ui-reviewer sol#medium
  ▸ 🐾 artist      sol#medium
  ▸ 🐾 writer      luna#high
  ▸ 🐾 researcher  luna#high
  objective     cost  (cheapest rung first)
  heavy slots   cpus/2  (for catherd lock)
  🐾 notify on milestone
  🐾 notify on finish
  🐾 notify on blocked
↑↓ move · → open · space ticks · / filter · enter save · q quit
```

### --plain, 80x24

```
 /\_/\  .  catherd
(=^.^=)/   herds coding agents
 (")(")    profiles - default

profile default 1/1 (active) - p next - n new - c copy - x delete
> > [x] architect   claude-opus-5-5#high
  > [x] verifier    claude-opus-5-5#low
  > [x] worker      luna#high, sol#medium, sol#high, sol#xhigh
  > [x] reviewer    sol#high
  > [x] ui-reviewer sol#medium
  > [x] artist      sol#medium
  > [x] writer      luna#high
  > [x] researcher  luna#high
  objective     cost  (cheapest rung first)
  heavy slots   cpus/2  (for catherd lock)
  [x] notify on milestone
  [x] notify on finish
  [x] notify on blocked
up/down move - -> open - space ticks - / filter - enter save - q quit
```

### --plain, 120x40

```
 /\_/\  .  catherd
(=^.^=)/   herds coding agents
 (")(")    profiles - default

profile default 1/1 (active) - p next - n new - c copy - x delete
> > [x] architect   claude-opus-5-5#high
  > [x] verifier    claude-opus-5-5#low
  > [x] worker      luna#high, sol#medium, sol#high, sol#xhigh
  > [x] reviewer    sol#high
  > [x] ui-reviewer sol#medium
  > [x] artist      sol#medium
  > [x] writer      luna#high
  > [x] researcher  luna#high
  objective     cost  (cheapest rung first)
  heavy slots   cpus/2  (for catherd lock)
  [x] notify on milestone
  [x] notify on finish
  [x] notify on blocked
up/down move - -> open - space ticks - / filter - enter save - q quit
```

### --reduced-motion, 80x24

```
 /\_/\  .  catherd
(=^.^=)/   herds coding agents
 (")(")    profiles · default

profile default 1/1 (active) · p next · n new · c copy · x delete
❯ ▸ 🐾 architect   claude-opus-5-5#high
  ▸ 🐾 verifier    claude-opus-5-5#low
  ▸ 🐾 worker      luna#high, sol#medium, sol#high, sol#xhigh
  ▸ 🐾 reviewer    sol#high
  ▸ 🐾 ui-reviewer sol#medium
  ▸ 🐾 artist      sol#medium
  ▸ 🐾 writer      luna#high
  ▸ 🐾 researcher  luna#high
  objective     cost  (cheapest rung first)
  heavy slots   cpus/2  (for catherd lock)
  🐾 notify on milestone
  🐾 notify on finish
  🐾 notify on blocked
↑↓ move · → open · space ticks · / filter · enter save · q quit
```

### --reduced-motion, 120x40

```
 /\_/\  .  catherd
(=^.^=)/   herds coding agents
 (")(")    profiles · default

profile default 1/1 (active) · p next · n new · c copy · x delete
❯ ▸ 🐾 architect   claude-opus-5-5#high
  ▸ 🐾 verifier    claude-opus-5-5#low
  ▸ 🐾 worker      luna#high, sol#medium, sol#high, sol#xhigh
  ▸ 🐾 reviewer    sol#high
  ▸ 🐾 ui-reviewer sol#medium
  ▸ 🐾 artist      sol#medium
  ▸ 🐾 writer      luna#high
  ▸ 🐾 researcher  luna#high
  objective     cost  (cheapest rung first)
  heavy slots   cpus/2  (for catherd lock)
  🐾 notify on milestone
  🐾 notify on finish
  🐾 notify on blocked
↑↓ move · → open · space ticks · / filter · enter save · q quit
```

## `catherd watch`, no runs yet
### normal, 80x24

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    watch · 0 runs

No runs yet. Start one with /catherd in Claude Code.
↑↓ move · enter details · q quit · refreshes every 2 s
```

### normal, 120x40

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    watch · 0 runs

No runs yet. Start one with /catherd in Claude Code.
↑↓ move · enter details · q quit · refreshes every 2 s
```

### --plain, 80x24

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    watch - 0 runs

No runs yet. Start one with /catherd in Claude Code.
up/down move - enter details - q quit - refreshes every 2 s
```

### --plain, 120x40

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    watch - 0 runs

No runs yet. Start one with /catherd in Claude Code.
up/down move - enter details - q quit - refreshes every 2 s
```

### --reduced-motion, 80x24

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    watch · 0 runs

No runs yet. Start one with /catherd in Claude Code.
↑↓ move · enter details · q quit · refreshes every 2 s
```

### --reduced-motion, 120x40

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    watch · 0 runs

No runs yet. Start one with /catherd in Claude Code.
↑↓ move · enter details · q quit · refreshes every 2 s
```

## `catherd init`, the Jev key prompt (intro already skipped)
### normal, 80x24

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    init 1/3: the Jev key

Paste your TypeSafe API key. Jev uses it to pick each lane's model and effort.
key:
```

### normal, 120x40

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    init 1/3: the Jev key

Paste your TypeSafe API key. Jev uses it to pick each lane's model and effort.
key:
```

### --plain, 80x24

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    init 1/3: the Jev key

Paste your TypeSafe API key. Jev uses it to pick each lane's model and effort.
key:
```

### --plain, 120x40

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    init 1/3: the Jev key

Paste your TypeSafe API key. Jev uses it to pick each lane's model and effort.
key:
```

### --reduced-motion, 80x24

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    init 1/3: the Jev key

Paste your TypeSafe API key. Jev uses it to pick each lane's model and effort.
key:
```

### --reduced-motion, 120x40

```
 /\_/\  .  catherd
(=-.-=)/   herds coding agents
 (")(")    init 1/3: the Jev key

Paste your TypeSafe API key. Jev uses it to pick each lane's model and effort.
key:
```

